name: server
description: A server app using the shelf package and Docker.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.3.3

dependencies:
  args: ^2.4.0
  logging: ^1.1.1
  shelf: ^1.4.0
  top_shelf: 1.0.0-alpha.6
  shelf_hotreload: ^1.4.0
  shelf_router: ^1.1.3
  sqlite3: ^2.4.3
  xml: ^6.2.2

dev_dependencies:
  http: ^1.1.0
  lints: ^3.0.0
  test: ^1.24.0
